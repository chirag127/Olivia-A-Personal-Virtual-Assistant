import os

"""
def translatelanguage(languageptext):
    query = takeCommand().lower()
    if languageptext in query:
        query = query.replace(languageptext, "")
        k = Translator().translate(query, dest=languageptext)
        translated = str(k.text)
        sp(translated)
"""
"""
speak("How long should i generate your password")

    def take_pass_length():
        while True:
            pass_len = takeCommand()
            if pass_len.isdigit():
                return pass_len
            else:
                speak("Please enter a valid number")
                take_pass_length()
pass_len = take_pass_length()
"""